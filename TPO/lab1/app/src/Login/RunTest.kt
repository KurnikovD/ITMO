package Login

class RunTest {
    fun init() {
        Login().init()
        Profile().init()
        Search().init()
        newHeader().init()
        newDashboard().init()
        Logout().init()
    }
}
